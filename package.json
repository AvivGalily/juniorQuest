{
  "name": "junior-quest",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy:prod": "node scripts/deploy-prod.mjs",
    "leaderboard:clear:prod": "node scripts/clear-prod-leaderboard.mjs",
    "invalidate:prod": "node scripts/deploy-prod.mjs --invalidate-only"
  },
  "dependencies": {
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
